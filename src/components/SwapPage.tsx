import { SwapWidget } from '@skip-go/widget';

const SwapPage = () => {
  return (
    <div
      style={{
        width: '450px',
        height: '640px',
      }}
    >
      <SwapWidget
        theme={{
          brandColor: '#FF4FFF',
        }}
      />
    </div>
  );
};

export default SwapPage;